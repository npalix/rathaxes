with Log
{
    template sequence Log::info(::string msg)
    {
        chunk Main::include_headers
        {
            #include <stdio.h>
            typedef int stdio_include_stamp;
        }

        chunk ::CALL
        {
            puts(${msg});
        }
    }
}
