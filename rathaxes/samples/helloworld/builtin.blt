with Builtin
{
    template type integer()
    {
        chunk ::decl()
        {
            typedef int t_integer;
        }

        chunk ::set(Number value)
        {
            ${self} = ${value};
        }

        map
        {
        }
    }
}
